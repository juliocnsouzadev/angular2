var simpleVar = 'simple var';

function simpleFunction(x, y){
    return x + y;
}

let letVar = 'let var';
const PI = 3.14

var numbers = [1,2,3]

numbers.map(v => v * 2);

class Math {
    sum(x, y) {return x+y}
}

var m: string = "new Math()";